//target document elements
$(___)

//create anonymous functions
$(selectSomething).anAction(function() {
});

var ___ = function(){
}

//make new document elements
var _____ = $('<__></__>');

// add strings together
'<p>' + '___' + '</p>'


//for each
.each()
//sense an event
.on()
//add to an array
.push()
//find an elements value
.val()
.val
//find an elements attribute of a certain type
.attr( __, __)
//add a class name
.addClass('___')
//access the innerHTML
.html()
//add something to the end of something else
.append()
//effect an elements text
.text('X')
//remove an item from an array
.splice(__, __)

//what was acted upon
.target
//the next thing up in the DOM
.parentElement
//an elements id
.id

//the object to which the code you are looking at right now applies -- called a “Keyword”
this